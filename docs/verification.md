# 验证记录

验证时间：2026-09-04

## 自动检查

| 检查 | 结果 |
|---|---|
| Python 编译 | `app.py`、推荐模块、评估模块和文档生成脚本通过 |
| 数据库初始化 | 已生成 5 个知识点、4 条图谱关系、50 道分层练习和 72 条确定性演示交互 |
| 离线评估 | 严格时间切分：`Precision@5=0.400`，`Recall@5=1.000`，`NDCG@5=0.553`，模型估计 `Knowledge Gain=0.029`；Learning Efficiency 在该演示留出期未达到 0.75 阈值，因此报告为未达成 |
| 图谱关联度 | GraphSAGE 使用方向感知前置/后继聚合、结构特征与 one-hot 节点身份；弱项邻域余弦匹配示例为 `0.799/0.685/0.831/0.543/0.530`，不再把单一嵌入维度截断为 1.00 |
| 时序模型 | `TemporalMasteryNet` 输出全部 5 个知识点的掌握概率；训练目标为下一时刻被作答知识点，对错、难度、用时、时间间隔进入 LSTM 与显式遗忘衰减 |
| Flask 测试客户端 | 仪表盘 200；答题页 200；提交答案 200；JSON 接口 200；提交表单计时写入 `duration_sec` |
| 学习功能路由 | 题库筛选、收藏、专项练习、错题本、学习记录、知识点详情全部返回 200 |
| 新交互闭环 | 每日目标可写入 SQLite；三题连续练习会显示第 1/3 题、提交后可进入第 2 题；题库即时搜索和排序控件已渲染 |
| 实际 HTTP | `http://127.0.0.1:5000/` 返回 200 |
| 文档结构 | DOCX 生成成功；PPTX 共 11 页 |
| PPT 渲染 | 已通过本机 PowerPoint 导出为 PDF 和 11 张 PNG；抽检封面、模型页与评估页，无文字裁切或重叠 |

## 已知边界

1. 运行环境内置的 Web 服务仅供本机演示，生产部署需要关闭 debug 并配置 WSGI 服务。
2. 演示数据用于功能验证，不能替代真实 ASSISTments/EdNet 实验；详见 `data_and_claims.md`。
